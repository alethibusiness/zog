You are adding new features to the zog CLI at /Users/adebimpeomolaso/Projects/zog.

## Tasks (in order)

### 1. Add `zog calendar` subcommands
Following the same patterns as `zog mail` in src/zog/commands/mail.py and src/zog/providers/zoho/mail.py:
- `zog calendar events list -a <email> [--calendar-id ID] [--start YYYY-MM-DD] [--end YYYY-MM-DD] [--max N] [-j|-p]`
- `zog calendar events get -a <email> <eventId> [-j|-p]`
- `zog calendar events create -a <email> --title ... --start ... --end ... [--description ...] [--location ...] [--attendees a@b.com,c@d.com]`
- `zog calendar list -a <email>` — list available calendars
Use Zoho Calendar API (https://calendar.zoho.com/api/v1). Required OAuth scopes: `ZohoCalendar.calendar.ALL,ZohoCalendar.event.ALL`.
Put code in: `src/zog/providers/zoho/calendar.py` and `src/zog/commands/calendar.py`.

### 2. Add `zog contacts` subcommands
- `zog contacts list -a <email> [--max N] [-j|-p]`
- `zog contacts get -a <email> <contactId> [-j|-p]`
- `zog contacts create -a <email> --name ... --email ... [--phone ...] [--company ...]`
Use Zoho Contacts / Zoho People API. Required scopes: `ZohoContacts.contactapi.READ,ZohoContacts.contactapi.ALL`.
Put code in: `src/zog/providers/zoho/contacts.py` and `src/zog/commands/contacts.py`.

### 3. Add `zog workdrive` subcommands (stub only — not fully implemented)
- `zog workdrive files list -a <email>` — list files in root
- `zog workdrive files get -a <email> <fileId>`
- `zog workdrive upload -a <email> <path> [--folder FOLDERID]`
Required scopes: `WorkDrive.files.ALL,WorkDrive.team.READ`.
Put code in: `src/zog/providers/zoho/workdrive.py` and `src/zog/commands/workdrive.py`.
Implement list + get fully, upload may be a stub raising NotImplementedError with a helpful message.

### 4. Update CLI wiring
Wire all three new subparsers into src/zog/cli.py, just like how mail is wired. Update help text.

### 5. Update scopes / OAuth flow
- When running `zog auth add`, the default scopes should now be:
  `ZohoMail.messages.ALL,ZohoMail.accounts.READ,ZohoMail.folders.READ,ZohoCalendar.calendar.ALL,ZohoCalendar.event.ALL,ZohoContacts.contactapi.READ,ZohoContacts.contactapi.ALL,WorkDrive.files.ALL,WorkDrive.team.READ`
- Add a `--services` CSV flag letting users pick fewer: `--services mail,calendar` → smaller scope set. Default is all.

### 6. Tests (all must pass)
Create these test files under tests/:
- `tests/test_calendar_mock.py` — mock requests, verify list/get/create events
- `tests/test_contacts_mock.py` — mock requests, verify list/get/create
- `tests/test_workdrive_mock.py` — mock requests, verify list/get (upload stub raises NotImplementedError cleanly)
- `tests/test_cli_help.py` — extend to check help for new subcommands

Run `pytest -q` and iterate until ALL tests pass. You already have `.venv` set up; use `.venv/bin/pytest`.

### 7. Version bump
- Bump `version = "0.1.0"` to `"0.2.0"` in pyproject.toml
- Bump `__version__ = "0.1.0"` to `"0.2.0"` in src/zog/__init__.py
- Add a 0.2.0 section to CHANGELOG.md summarising the new features

### 8. Build distribution artifacts
Run:
```
.venv/bin/pip install --upgrade build
.venv/bin/python -m build  # produces dist/zog-cli-0.2.0.tar.gz and dist/zog_cli-0.2.0-py3-none-any.whl
```
Verify both files exist. Do NOT upload to PyPI.

### 9. Commit
Make one logical commit per section if it helps, or one clean commit for the whole feature release. Use clear messages. Do NOT push — Claude will push.

### 10. Final summary
Print a concise summary: files created/modified, test count, build output paths, any deviations from spec, and which scopes the user needs to re-grant for live testing to work end-to-end.

Operational rules:
- Python 3.10+ only, stdlib + requests + platformdirs.
- Keep files small and focused.
- Match existing code style (argparse, type hints, docstrings).
- All tests must pass before you finish.
- Do NOT push to remote.

Begin.
